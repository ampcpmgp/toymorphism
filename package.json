{
  "name": "toymorphism-portal",
  "private": true,
  "description": "Toy + Skeuomorphism",
  "main": "src/index.js",
  "engines": {
    "node": ">=16.0.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npm run dev --workspace=website",
    "build": "npm run build --workspace=website",
    "storybook": "npm run storybook --workspace=svelte",
    "storybook:ce": "npm run storybook --workspace=custom-elements",
    "storybook:r": "npm run storybook --workspace=react",
    "lint": "npm run lint --workspaces",
    "format": "npm run format --workspaces",
    "format:diff": "npm run format:diff --workspaces",
    "release": "changeset publish"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ampcpmgp/toymorphism.git"
  },
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/ampcpmgp/toymorphism/issues"
  },
  "homepage": "https://ampcpmgp.github.io/toymorphism/",
  "devDependencies": {
    "@changesets/cli": "^2.17.0",
    "eslint": "^7.32.0",
    "eslint-plugin-svelte3": "^3.2.1",
    "prettier": "^2.4.1",
    "prettier-plugin-svelte": "^2.4.0"
  },
  "dependencies": {
  },
  "workspaces": [
    "packages/*",
    "website"
  ]
}
